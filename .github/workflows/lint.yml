name: Lint & Code Quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  yaml-lint:
    name: Lint YAML Files
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run yamllint
        uses: ibiqlik/action-yamllint@v3
        with:
          config_file: .yamllint.yml
          file_or_dir: |
            .github/workflows/
            docker/compose*.yaml

  shell-check:
    name: Check Shell Scripts
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: './docker/scripts'
          severity: warning

  python-lint:
    name: Lint Python Code
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install linting tools
        run: |
          pip install ruff black isort

      - name: Run ruff
        run: |
          # Advisory mode: linting failures don't block CI (cost optimization)
          ruff check utils/ || true

      - name: Check code formatting with black
        run: |
          # Advisory mode: formatting issues don't block CI (cost optimization)
          black --check utils/ || true

      - name: Check import sorting
        run: |
          # Advisory mode: import issues don't block CI (cost optimization)
          isort --check-only utils/ || true

  markdown-lint:
    name: Lint Markdown
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run markdownlint
        uses: nosborn/github-action-markdown-cli@v3.5.0
        with:
          files: .
          config_file: .markdownlint.json
          ignore_files: data/
